#include<bits/stdc++.h>
using namespace std;
int main()
{
	int t,a,len;
	string s;
    scanf("%d",t);
    while (t--)
    {
        scanf("%s",&s);
        len=s.length();
        for (int i=0; i<len-1;i++)
        {
        	a+=1;
		}

    }
}
